#include <iostream>

int main(void)
{
	int m, n, t;
	std::cin >> m >> n >> t;
	switch(t)
	{
		case 1:
		int facto = 1;
		for(int i = n; i > 0; --i)
		{
			if(facto > m){
				TLE();
				return 0;
			}
			factorial *=i;
		}
		break;
		
	}
}